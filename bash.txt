#!/bin/bash

for i in $(seq $start_range $end_range); >
ip = "{network_prefix}${i}" 
if ping -c 2 -W 1 "$ip" > /dev/null 2>&1; 
then echo "$ip is active" 
else echo "$ip is not active" 
fi done
